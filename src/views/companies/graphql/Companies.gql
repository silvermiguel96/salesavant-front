query AllCompanies($first: Int, $offset: Int) {
  companies(first: $first, offset: $offset) {
    totalResults
    companiesList {
      uid
      name
      status
      description
      city
      state
      country
      numEmployees
      momentum
      url
      vertical
      totalScore
      totalSignals
    }
  }
}
