query AllSignals($search: String, $first: Int, $offset: Int) {
  signals(search: $search first: $first offset: $offset) {
    totalResults
    signalsList{
      id
      userId
      user {
        id
      }
      account {
        id
      }
      name
      group
      category
      accountId
      description
      creationTime
      defaultScore
      modificationTime
    }
  }
}
