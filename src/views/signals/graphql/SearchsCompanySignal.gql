query getSignalsById($signalId: Int, $first: Int, $offset: Int) {
  signalCompanies(signalId: $signalId, first:$first , offset: $offset) {
    totalResults
    companiesList{
      uid
      name
    }
  }
}