query AllPlaylists(
  $first: Int,
  $offset: Int,
  $sortBy: String,
  $sortOrder: String
) {
  playlists(first: $first, offset: $offset, sortBy: $sortBy, sortOrder: $sortOrder ) {
    totalResults
    playlistsList{
      uid
      name
      creationTime
      accountId
      userId
      totalSignals
      totalCompanies
      totalScore
    }
  }
}
