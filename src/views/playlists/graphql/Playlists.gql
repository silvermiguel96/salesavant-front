query AllPlaylists($first: Int, $offset: Int) {
  playlists(first: $first, offset: $offset) {
    uid
    name
    creationTime
    accountId
    userId
    signals {
      id
      comments
    }
    companies {
      uid
      name
    }
  }
}
