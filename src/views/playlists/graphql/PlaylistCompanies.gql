query SinglePlaylist($uid: String $first: Int $offset: Int) {
  companies(playlistUid: $uid first: $first offset: $offset){
    uid
    name
    description
    city
    state
    country
    momentum
    numEmployees
    website
    url
    vertical
    totalScore
    totalSignals
  }
}
